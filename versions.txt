# PACTS v1.2 Production Toolchain Versions
# Pin these versions for reproducible builds and deterministic behavior

## Runtime
Python==3.11.9
Node>=18.0.0  # For Playwright browsers

## Core Dependencies
playwright==1.40.0
pytest-playwright==0.4.3

## LLM & AI
anthropic==0.69.0
openai==1.109.1
langchain==0.3.26
langchain-core==0.3.68
langchain-anthropic==0.3.17
langchain-openai==0.3.27
langchain-community==0.3.27
langgraph==0.4.10
langgraph-checkpoint==2.1.0

## MCP Integration
langchain-mcp-adapters==0.1.8
mcp==1.9.0

## Browsers (Playwright)
# Use: playwright install chromium firefox webkit --with-deps
# Version bundled with playwright==1.40.0:
# - Chromium 121.0.6167.57
# - Firefox 121.0
# - WebKit 17.4

## Environment
USE_MCP=true
MCP_ACTIONS_ENABLED=false  # Discovery only, no actions
SLOW_MO=0  # Production speed (use 800 for debugging)

## Determinism
# No random sampling in planners
# Seeded randomness if any (use RANDOM_SEED=42)
# Fixed browser viewport: 1280x720

## CI/CD Requirements
- GitHub Actions: ubuntu-latest (22.04)
- Headless mode: chromium only (fastest)
- Screenshot capture: on failure + success
- Trace collection: on failure only
- Retry policy: rerun-on-fail once, max 1 retry per test
