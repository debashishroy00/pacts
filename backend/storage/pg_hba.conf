# PostgreSQL Client Authentication Configuration File
# TYPE  DATABASE  USER        ADDRESS                METHOD

# Unix domain socket connections (inside container)
local   all       all                                trust

# IPv4 local connections (localhost)
host    all       all         127.0.0.1/32           scram-sha-256

# IPv6 local connections (localhost)
host    all       all         ::1/128                scram-sha-256

# Docker Desktop gateway subnet (Windows host connections)
host    all       all         192.168.65.0/24        scram-sha-256

# Docker internal hostname
host    all       all         172.16.0.0/12          scram-sha-256

# Allow all other connections (dev only - tighten for production)
host    all       all         0.0.0.0/0              scram-sha-256
